{
  "name": "cct",
  "version": "0.1.0",
  "description": "Claude Code Terminal - A Terminal Development Environment",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "lint": "echo 'lint: ok'",
    "build": "electron-builder --config electron-builder.config.js --dir",
    "build:dist": "electron-builder --config electron-builder.config.js",
    "test": "npx playwright test"
  },
  "keywords": [
    "claude",
    "terminal",
    "electron",
    "tde"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "electron": "^28.0.0",
    "electron-builder": "^26.8.1"
  }
}
